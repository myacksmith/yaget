# Example configuration for YAGET

# GitLab version for deployments using the default template
GITLAB_VERSION=latest
# GITLAB_VERSION=16.0.0-ee.0
# GITLAB_VERSION=15.11.3-ce.0

# Custom artifacts directory (default: ./artifacts)
# YAGET_ARTIFACTS_ROOT=/mnt/large-disk/yaget-artifacts

# Custom templates directory (default: ./templates)  
# YAGET_TEMPLATES_DIR=/shared/gitlab-templates

# Common deployment variables
EXTERNAL_URL=http://gitlab.local
# GITLAB_ROOT_PASSWORD=mysecurepassword
# HTTP_PORT=8080
# HTTPS_PORT=8443
# SSH_PORT=2222

# LDAP configuration (for sso template)
# LDAP_BIND_DN=cn=admin,dc=example,dc=org
# LDAP_BIND_PASSWORD=ldappassword
# LDAP_BASE=dc=example,dc=org
