# Default .env file for gitlab-compose
# Contains minimal default values that can be used by all deployments
# Variables can be overridden by service-specific .env files

# GitLab Version Settings
# Can be overridden with --version flag when running deploy.sh
GITLAB_VERSION=latest

# Note: The following variables are automatically set by the deploy script:
# SERVICE_NAME, CONTAINER_NAME, DEPLOYMENT_NAME, NETWORK_NAME, 
# CONFIG_PATH, SERVICE_DIR, HTTP_PORT, HTTPS_PORT, SSH_PORT